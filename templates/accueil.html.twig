{% extends "base.html.twig" %}

{% block description %}<meta name="description" content="Accueil">{% endblock %}
{% block title %}Accueil{% endblock %}

{% block main %}
    
        {# Flashes utilisateurs #}

    {% for type, messages in {
        'user_edit_ok': 'alert-warning',
        'user_delete_ok': 'alert-danger',
        'user_add_success': 'alert-success'
    } %}
        {% for message in app.flashes(type) %}
            <div class="container col-md-6 mt-5">

            <div class="alert {{ messages }} alert-dismissible fade show" role="alert">
                <strong>Félicitations !</strong> {{ message }}
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
            </div>
        {% endfor %}
    {% endfor %}

{#        {% for message in app.flashes('user_edit_ok') %}#}
{#        <div class="alert alert-warning alert-dismissible fade show" role="alert">#}
{#            <strong>Félicitations !</strong> {{ message }}#}
{#            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>#}
{#        </div>#}
{#        {% endfor %}#}

{#        {% for message in app.flashes('user_delete_ok') %}#}
{#            <div class="alert alert-danger alert-dismissible fade show" role="alert">#}
{#                <strong>Félicitations !</strong> {{ message }}#}
{#                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>#}
{#            </div>#}
{#        {% endfor %}#}

{#        {% for message in app.flashes('user_add_success') %}#}
{#            <div class="alert alert-success alert-dismissible fade show" role="alert">#}
{#                <strong>Félicitations !</strong> {{ message }}#}
{#                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>#}
{#            </div>#}
{#        {% endfor %}#}


{#        #}{#  Flashes produits #}

{#        {% for message in app.flashes('products_add_success') %}#}
{#            <div class="alert alert-success alert-dismissible fade show" role="alert">#}
{#                <strong>Félicitations !</strong> {{ message }}#}
{#                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>#}
{#            </div>#}
{#        {% endfor %}#}

{#        {% for message in app.flashes('product_edit_success') %}#}
{#            <div class="alert alert-success alert-dismissible fade show" role="alert">#}
{#                <strong>Félicitations !</strong> {{ message }}#}
{#                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>#}
{#            </div>#}
{#        {% endfor %}#}


        <h1>Bienvenue sur natation-eco.fr</h1>
        <section class="club ctn_centre">
            <h6 class="d-none">Titre caché</h6>
            <article>
                <h6 class="d-none">Titre caché</h6>
                <p class="contenant">
                    <a href="{{ path('club') }}"><img src="{{ asset("media/club.webp")}}" alt="le club" class="accueil-img"></a>
                    <a class="texte_centrer menu nav-link" href="{{ path('club') }}">Le club</a>
                </p>
            </article>
        </section>
        <section class="ctn_centre">
            <h6 class="d-none">Titre caché</h6>
            <p class="contenant">
                <a href="{{ path('femme_show', {id: 2}) }}"><img src="{{ asset("media/article-femme.webp")}}" alt="maillots de bain femme" class="accueil-img"></a>
                <a class="texte_centrer menu nav-link nav-link" href="{{ path('femme_show', {id: 2}) }}">Maillots de bain femme</a>
            </p>
            <p class="contenant">
                <a href="{{ path('homme_show', {id: 1}) }}"><img src="{{ asset("media/article-homme.webp")}}" alt="maillots de bain homme" class="accueil-img"></a>
                <a class="texte_centrer menu nav-link" href="{{ path('homme_show', {id: 1}) }}">Maillots de bain homme</a>
            </p>
        </section>
        <section class="ctn_centre">
            <h6 class="d-none">Titre caché</h6>
            <p class="contenant">
                <a href="{{ path('nutrition_show',{id: 3}) }}"><img src="{{ asset("media/nutrition.webp")}}" alt="nutrition" class="accueil-img"></a>
                <a class="texte_centrer menu nav-link" href="{{ path('nutrition_show',{id: 3}) }}">Nutrition</a>
            </p>
            <p class="contenant">
                <a href="{{ path('equipement_show', {id:4}) }}"><img src="{{ asset("media/equipement.webp")}}" alt="equipement" class="accueil-img"></a>
                <a class="texte_centrer menu nav-link" href="{{ path('equipement_show', {id:4}) }}">Equipement</a>
            </p>
        </section>
        <hr>
        <h3 class="mt-5">Ces articles pourraient vous intéresser</h3>
        <section>
            <h6 class="d-none">Titre caché</h6>
            <hr>
        </section>
        {# ========== Carousel ========== #}

            <aside id="carousel" class="carousel mb-5" data-flickity='{ "autoPlay": true, "wrapAround": true, "autoPlay": true, "cellAlign": "center", "contain": true, "pageDots": false, "lazyLoad": true }'>
            {% for products in products %}
                <aside class="card carousel-cell" style="width: 18rem;">
                    <img src="{{ vich_uploader_asset(products, 'imageFile') }}" class="card-img-top" alt="image d'article" >
                    <aside class="card-body">
                        <h5 class="card-title">{{ products.name}}</h5>
                        <p>Prix : {{ products.price }} €</p>
                        <a href="{{ path('products_show', {id: products.id}) }}" class="btn rounded-pill command">Détails</a>
                    </aside>
                </aside>
            {% endfor %}
        </aside>
        {# ========== Carousel ========== #}
{% endblock %}
{% block footer %}
<script>
window.axeptioSettings = {
  clientId: "63564a6e8ad4f3b94816c6f1",
  cookiesVersion: "github/skywon01/natation-eco-fr",
};
 
(function(d, s) {
  var t = d.getElementsByTagName(s)[0], e = d.createElement(s);
  e.async = true; e.src = "//static.axept.io/sdk.js";
  t.parentNode.insertBefore(e, t);
})(document, "script");
</script>{% endblock %}